name: Temporary 0.12.4 general event weather

on:
  push:
    branches:
      - feature/general-event-weather-0.12.4
    paths:
      - .github/workflows/tmp-0124.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/general-event-weather-0.12.4

      - name: Apply staged source patch
        run: |
          python3 .github/tmp-0124.py
          python3 .github/tmp-0124-fix.py

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Install Android SDK
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-36' 'build-tools;35.0.0'

      - name: Unit tests, lint and debug build
        working-directory: Android
        run: gradle --no-daemon testDebugUnitTest lintRelease assembleDebug

      - name: Commit tested changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Android/app/build.gradle \
                  Android/app/src/main/java/ch/pfvr/internapp/MainActivity.java \
                  Android/app/src/main/java/ch/pfvr/internapp/WeatherEventPolicy.java \
                  Android/app/src/test/java/ch/pfvr/internapp/WeatherEventPolicyTest.java \
                  Android/app/src/test/java/ch/pfvr/internapp/WeatherEventSourceTest.java \
                  Android/app/src/test/java/ch/pfvr/internapp/ExternalContentLocalizationSourceTest.java \
                  Android/app/src/test/java/ch/pfvr/internapp/WeatherForecastSourceTest.java \
                  PROJECT.md STATUS.md
          git diff --cached --check
          git commit -m 'Use next relevant club event for weather'
          git push origin HEAD:feature/general-event-weather-0.12.4
