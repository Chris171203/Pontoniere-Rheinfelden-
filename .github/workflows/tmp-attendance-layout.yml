name: Temporary attendance layout patch

# rerun: preserve existing two-column line-clamp while polishing labels
on:
  push:
    branches:
      - fix/internal-attendance-label-layout
    paths:
      - .github/workflows/tmp-attendance-layout.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/internal-attendance-label-layout

      - name: Apply staged source patch
        run: python3 .github/tmp-attendance-layout.py

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.13'

      - name: Install Android SDK
        shell: bash
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager 'platforms;android-36' 'build-tools;35.0.0'

      - name: Unit tests and compile
        working-directory: Android
        run: gradle --no-daemon testDebugUnitTest assembleDebug

      - name: Commit tested changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add Android/app/build.gradle \
                  Android/app/src/main/java/ch/pfvr/internapp/InternalAttendanceSkin.java \
                  Android/app/src/test/java/ch/pfvr/internapp/InternalAttendanceUiRegressionTest.java \
                  STATUS.md
          git diff --cached --check
          git commit -m 'Polish attendance names and status labels'
          git push origin HEAD:fix/internal-attendance-label-layout
