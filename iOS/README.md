# iOS

Reserviert für die spätere native iOS-App.

Ziel ist funktionale Parität mit der Android-App, ohne Android-spezifische Implementierungsdetails zu übernehmen. Gemeinsame fachliche Regeln und Datenquellen werden auf Repository-Ebene dokumentiert.

Noch kein iOS-Projekt angelegt.
